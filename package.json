{
  "name": "fifalog",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "common",
    "backend",
    "frontend",
    "migrations"
  ],
  "scripts": {
    "start:dev": "concurrently -n frontend,backend 'yarn workspace frontend start:dev' 'yarn workspace backend start:dev'",
    "migrate-db:dev": "ts-node migrations/migrate.ts",
    "build": "concurrently -n frontend,backend 'yarn workspace frontend build' 'yarn workspace backend build' 'yarn workspace migrations build'",
    "start": "NODE_ENV=production node dist/backend/src/main.js",
    "migrate-db": "node dist/migrations/migrate.js",
    "lint": "yarn workspace backend lint"
  },
  "devDependencies": {
    "concurrently": "^7.1.0",
    "prettier": "^2.6.2"
  }
}
